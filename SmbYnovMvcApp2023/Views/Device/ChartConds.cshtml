@model DataToDisplay

@{
    var myChart = new System.Web.Helpers.Chart(width: 600, height: 400)
        .AddTitle("Condensation")
        .AddSeries(
            name: "Condensation",
            chartType: "Line",
            yValues: new[] { Model.valueCond },
            xValue: new[] { Model.dateCond })
        .Write();
}