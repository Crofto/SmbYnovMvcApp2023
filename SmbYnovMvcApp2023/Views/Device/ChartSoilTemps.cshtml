@model DataToDisplay

@{
    var myChart = new  System.Web.Helpers.Chart(width: 600, height: 400)
        .AddTitle("Temp sol")
        .AddSeries(
            name: "Temp sol",
            chartType: "Line",
            yValues: new[] { Model.valueTempSoil },
            xValue: new[] { Model.dateTempSoil })
        .Write();
}